spring:
  application:
    name: student-service

  datasource:
    url: jdbc:postgresql://localhost:5432/university
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 9090